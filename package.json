{
  "name": "passwordmanagement",
  "version": "1.0.0",
  "private": true,
  "description": "纯前端的随机密码 / Token / UUID 生成器，部署在 Cloudflare Workers 静态资源上。",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
